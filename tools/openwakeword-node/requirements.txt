# openWakeWord declares tflite-runtime on Linux, which often has no wheel for newer Python.
# Run: npm run setup   (installs deps then: pip install openwakeword --no-deps)
onnxruntime>=1.16.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
requests>=2.28.0
tqdm>=4.65.0
